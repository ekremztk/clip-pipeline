# ── Web server ────────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-dotenv==1.2.2
pydantic

# ── Database ──────────────────────────────────────────────────────────────────
supabase
psycopg2-binary

# ── Google AI / GCS ───────────────────────────────────────────────────────────
google-genai
google-cloud-storage
google-auth

# ── Vision / Detection ────────────────────────────────────────────────────────
# MediaPipe — CPU-only TFLite backend (BlazeFace fallback engine)
mediapipe>=0.10.0

# YOLOv8 — CUDA path is used when torch detects a GPU automatically.
# NOTE: Install PyTorch with CUDA BEFORE installing ultralytics.
# On g4dn.xlarge (T4, CUDA 12.x) run this first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Then: pip install ultralytics>=8.1.0
ultralytics>=8.1.0

# OpenCV — headless (no GUI deps needed on a server)
opencv-python-headless

# Numerical
numpy
Pillow

# ── Storage (R2 debug video uploads) ─────────────────────────────────────────
boto3

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx>=0.27.0
requests

# ── Observability (optional — safe to omit if keys not set) ──────────────────
langfuse>=2.0.0,<3.0.0
